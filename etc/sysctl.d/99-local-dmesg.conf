# Allow unprivileged users to read kernel messages with dmesg.
#
# This is convenient on personal/debug systems, but lowers the default
# kernel message privacy/hardening level. Remove this file on shared or
# security-sensitive systems.

kernel.dmesg_restrict = 0
