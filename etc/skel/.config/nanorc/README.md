# nano syntax highlighting
This directory contains optional GNU nano syntax definitions used by `~/.nanorc`.

The user-level nano config includes them with:

```nanorc
include "~/.config/nanorc/*.nanorc"
```
